import { SampleButton } from './SampleButton';
import { SampleCounter } from './SampleCounter';

export const Sample = () => {
  return (
    <div>
      <SampleCounter />
      <SampleButton />
    </div>
  );
};
