{
  "name": "poc",
  "version": "1.0.0",
  "author": "Oiram Aran",
  "license": "ISC",
  "private": "true",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "clean": "rm -rf yarn.lock package-lock.json && find . -name 'node_modules' -exec rm -rf {} +",
    "prepare": "husky",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.1",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.3",
    "turbo": "^2.0.7"
  }
}
